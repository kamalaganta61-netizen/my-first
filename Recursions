# Recursion is a function calling itself directly or indirectly.
# structure of a recirsive function

def function_name():
    if (based_condition):
        return Value         # stop recursion
    else:
        # recursive call
        function_name()


# example:1 Recursive function to print numbers

def print_numbers(n):
    if n == 0:   #based conditon
        return
    else:
        print(n)
        print_numbers(n - 1)      # recursive call

print_numbers(5)


# exmaple 2: factorial using recursion

def factorial(n):
    if n == 1:
        return 1
    else:
        return n * factorial(n - 1)
    
print(factorial(5))